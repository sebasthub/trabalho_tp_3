@model IEnumerable<Cinemaxx.programacao>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.filme1.nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.sala1.indentificador)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.horario)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.filme1.nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.sala1.indentificador)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.horario)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
